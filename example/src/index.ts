import simple from './simple';
import component from './component';
import context from './context';
import counters from './counters';
import order from './order';
import todoReact from './todo-react';

const EXAMPLES = {
  simple,
  component,
  context,
  counters,
  order,
  todoReact,
};

export default EXAMPLES;
